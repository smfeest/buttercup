name: Web API / CI
on:
  push:
  workflow_dispatch:
jobs:
  build:
    name: Build and test
    runs-on: ubuntu-20.04
    env:
      WEB_PROJECT_PATH: src/Buttercup.Web
    steps:
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set SHORT_COMMIT_SHA
        run: echo "SHORT_COMMIT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
      - name: Restore .NET tools
        run: dotnet tool restore
      - name: Restore .NET dependencies
        run: dotnet restore
      - name: Restore npm cache
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
      - name: Restore Node.js dependencies
        run: npm ci
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Build
        run: dotnet build --no-restore --configuration Release
      - name: Start MySQL Server
        run: sudo systemctl start mysql.service
      - name: Create MySQL users
        run: mysql -uroot -proot < db/dev-user.sql
      - name: Run .NET tests
        run: dotnet test --no-build --configuration Release --collect "XPlat Code Coverage"
      - name: Report .NET test coverage
        if: ${{ hashFiles('**/coverage.cobertura.xml') }}
        run: |
          dotnet reportgenerator -reports:**/coverage.cobertura.xml -targetdir:CoverageReports -reporttypes:TextSummary
          cat CoverageReports/Summary.txt
      - name: Run Jest tests
        run: npx jest --coverage
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Check formatting
        run: npx prettier -c .
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Lint scripts
        run: npx eslint .
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Lint styles
        run: npx stylelint --max-warnings 0 styles/
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Build assets
        run: npx gulp build
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Publish
        run: dotnet publish Buttercup.Web.csproj --no-build --no-restore --configuration Release --output publish-output
        working-directory: ${{env.WEB_PROJECT_PATH}}
      - name: Upload
        uses: actions/upload-artifact@v2
        with:
          name: buttercup-web-${{env.SHORT_COMMIT_SHA}}
          path: ${{env.WEB_PROJECT_PATH}}/publish-output
